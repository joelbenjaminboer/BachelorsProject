device: auto

deterministic: false

autocast:
  enabled: true
  dtype: bfloat16

compile:
  enabled: true
  mode: max-autotune
  fullgraph: false
  dynamic: false
  backend: null

parallel:
  strategy: auto

cuda:
  tf32: true
  cudnn_benchmark: true
  cudnn_deterministic: false
  allow_fused_adamw: true

gradient:
  accumulation_steps: 1
  clip_grad_norm: null
