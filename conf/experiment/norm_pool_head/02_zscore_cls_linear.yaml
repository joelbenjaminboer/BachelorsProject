# @package _global_
# zscore normalisation, CLS pooling, linear regression head.

version: "nph_02_zscore_cls_linear"

run:
  preprocess: false
  pretrain: false
  train: true
  eval: true

training:
  epochs: 20

model:
  encoder:
    pooling: cls
    head_type: linear
