# @package _global_

defaults:
  - override /model: timesnet

dataset:
  processed_dir: data/processed/ENABL3S_100hz_both
  original_freq: 500
  target_freq: 100
  use_both_legs: true

training:
  context_length: 25
  forecast_horizon: 10

version: "timesnet_100hz_both"
